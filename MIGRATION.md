# Migration Guide

A v2 rewrite was attempted but reverted, the library is back to a clean, simple API. There is no migration needed from a previous version; start fresh with `auth.New` as documented in the README.
